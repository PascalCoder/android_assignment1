package com.example.firstassignment.utils;

public class StringUtils {

    public static String formatDate(int month, int dayOfMonth, int year) {
        return month + 1 + "/" + dayOfMonth + "/" + year;
    }
}
